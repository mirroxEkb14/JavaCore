package Dates.NewDateTimeApi;

public class Advantages {

    /**
     * Months start with 1, everything is in one package
     *
     * Immutable classes: each action with an object returns
     * a new object
     *
     * Method Chaining: perform lots of transformations shortly:
     *      ZonedDateTime zdt = LocalDateTime.now()
     *          .plusHours(1)
     *          .atZone(ZoneId.of("PST"));
     */
}
