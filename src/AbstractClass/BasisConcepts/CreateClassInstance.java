package AbstractClass.BasisConcepts;

public abstract class CreateClassInstance {

    /**
     * We cannot create instance of abstract class because
     * abstract class are not finished, they might have methods
     * without realization, so we must set this realization in
     * class heirs. And if class is not ready for using, so that
     * we cannot create instance of such class
     *
     * CreateClassInstance cci = new CreateClassInstance(); // cannot be instantiated
     *
     * But we can create instances of heirs of abstract classes
     */
}
